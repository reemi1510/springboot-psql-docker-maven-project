package com.project.template.service.impl;

/*@Service
public class UserServiceImpl implements UserService {

    @Autowired
    SomeRepository someRepository;

    @Override
    public Response methodThatDealsWith(CreationRequest request) {

        Entity newEntity = new Entity(Get details from request);

        //Maybe throw an exception if something is invalid here

        Entity entity = someRepository.save(newEntity);

        return new Response(entity);
    }
}*/
