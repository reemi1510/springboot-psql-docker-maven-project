package com.project.template.service;

import com.project.template.controller.request.CreationRequest;
import com.project.template.controller.response.Response;

public interface SomeService {
    Response methodThatDealsWith(CreationRequest request);
}
